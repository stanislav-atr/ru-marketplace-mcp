[project]
name = "lamoda-connector"
version = "2.4.2"
description = "Lamoda MCP connector — GraphQL SKU enrichment plus CDP-backed search"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Vladimir-Human", email = "39053957+Vladimir-Human@users.noreply.github.com" }]
dependencies = [
    # Pinned, not floating: the name `mcp-core` on PyPI belongs to an
    # unrelated project. Unpinned, a wheel installed straight from a GitHub
    # Release resolves to that stranger's package and the server dies on
    # import. A pin turns a silent wrong install into a loud resolve failure.
"mcp-core==2.4.2",
    "fastmcp>=3.4.6,<4",
    "httpx>=0.27",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "playwright>=1.40",
    # lamoda_images composes a labelled contact sheet: one image whose tiles
    # carry their SKU, because some clients drop the text between images.
    "pillow>=10.1",
]

[project.scripts]
lamoda-mcp = "lamoda_connector.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lamoda_connector"]

[tool.uv.sources]
mcp-core = { workspace = true }
